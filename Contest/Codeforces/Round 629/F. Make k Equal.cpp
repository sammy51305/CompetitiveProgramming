#include<bits/stdc++.h>
#define endl '\n'
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, k;
    cin >> n;
    vector<int> nums(n);
    for(int i = 0;i < n; i++){
        cin >> nums[i];
    }
    sort(v.begin(), v.end());
    return 0;
}
